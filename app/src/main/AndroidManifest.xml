<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml
    Define permisos, actividades y configuración de la app.
    Para comunicarse con impresoras en red local (Epson L3560 vía IPP/eSCL)
    necesitamos permisos de red y, para mDNS, de ubicación.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ===== PERMISOS DE RED ===== -->
    <!-- Acceso a internet para comunicar con impresora vía IPP/eSCL -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Estado de red WiFi -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- Necesario para mDNS/NSD (Network Service Discovery) -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <!-- Requerido por Android para descubrir servicios mDNS en la red local -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- ===== PERMISOS DE ALMACENAMIENTO ===== -->
    <!-- minSdk = 33: solo aplican los permisos granulares de Android 13+.
         Solo imágenes: la app imprime PDF/JPEG/PNG, no usa video. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- ===== PERMISOS DE NOTIFICACIONES ===== -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ===== CAPACIDADES REQUERIDAS ===== -->
    <uses-feature
        android:name="android.hardware.wifi"
        android:required="true" />

    <application
        android:name=".EpsonPrintApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EpsonPrint"
        android:usesCleartextTraffic="true">
        <!--
            IMPORTANTE: android:usesCleartextTraffic="true"
            La Epson L3560 usa HTTP (no HTTPS) en la red local:
            IPP en puerto 631 y eSCL en puerto 80 son HTTP plano.
            Sin esto, Android bloquearía las conexiones desde Android 9+.
        -->

        <!-- ===== ACTIVIDAD PRINCIPAL ===== -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
